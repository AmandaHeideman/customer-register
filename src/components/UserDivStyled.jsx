import styled from 'styled-components';
import colors from '../Colors';

const UserDivStyled = styled.div`
    padding: 20px;
    background-color: ${colors.main};
    width: fit-content;
    color: ${colors.light};
    border-radius: 10px;
`

export default UserDivStyled